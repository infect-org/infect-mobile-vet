import styleDefinitions from '../../helpers/styleDefinitions.js';

/**
 * Shared styles for guideline header (in navigation)
 */
export default {
    headerStyle: {
        backgroundColor: styleDefinitions.colors.resistances.darkBackground,
    },
};
